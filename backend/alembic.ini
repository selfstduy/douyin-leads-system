# A generic, single database configuration.

[alembic]
# path to migration scripts
script_location = alembic

# sys.path prepended when running migrations
prepend_sys_path = .

# the output encoding used when revision files are written
output_encoding = utf-8

# Connection string – override via env.py or DATABASE_URL env var
sqlalchemy.url = postgresql+asyncpg://postgres:postgres@localhost:5432/douyin_leads

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
